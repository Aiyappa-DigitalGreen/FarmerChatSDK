<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Legacy image access for Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application>
        <activity
            android:name="com.farmerchat.sdk.activity.FarmerChatActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <!-- Home-screen widget — host app must add this receiver to its own manifest -->
        <receiver
            android:name="com.farmerchat.sdk.FarmerChatWidgetProvider"
            android:exported="true"
            android:label="FarmChat AI">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/farmerchat_widget_info" />
        </receiver>

        <provider
            android:name="com.farmerchat.sdk.provider.SdkFileProvider"
            android:authorities="${applicationId}.farmerchat.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/farmerchat_file_paths" />
        </provider>
    </application>

</manifest>
